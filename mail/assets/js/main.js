// js di mail_checker

// userMail
var userMail = prompt("Salve, qual è la tua Email?");
console.log(userMail);

// check email list
if (userMail = ) {

}

// frase stampata esempio
document.getElementById('mail_checker').innerHTML = "access denied";